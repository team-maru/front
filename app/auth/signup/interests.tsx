import CustomText from "@/components/ui/CustomText";
import { SafeAreaView } from "react-native-safe-area-context";

export default function InterestsScreen() {
  return (
    <SafeAreaView>
      <CustomText>관심사 고르기</CustomText>
    </SafeAreaView>
  );
}
